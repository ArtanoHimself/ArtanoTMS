
import Foundation

struct MainScreenModels {
    
    struct CounterModel {
        let count: Int
    }
    
    struct Request {
        let isValid: Bool
    }
    
    struct Response {
        let newCount: Int
    }
}
